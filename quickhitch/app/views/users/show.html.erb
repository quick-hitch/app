<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

 <div class="span8">
    <% if @user.conferences.any? %>
      <h3>conferences (<%= @user.conferences.count %>)</h3>
      <ol class="conferences">
        <%= render @conferences %>
      </ol>
      <%= will_paginate @conferences %>
    <% end %>
  </div>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
